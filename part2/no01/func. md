# 자바스크립트 함수 업그레이드하기 (default parameter/arguments)
- 디폴트 파라미터
  - 수학 연산자도 가능
  - 다른 파라미터로 바로 연산 가능
  - 다른 함수를 바로 실행가능(리턴값이 날아옴)

- `arguments` : 함수의 파라미터들을 한꺼번에 다룸.
```javascript
function 함수(a,b,c){
  for (var i = 0; i < arguments.length; i++){
    console.log(arguments[i])
  }
}

함수(2,3,4);
```